version: "3.6"
services:
  sut:
    build:
      context: ./
      args:
        PORT: 3000
        NODE_ENV: test
        VERSION: test
    command: ["npm", "test"]
    volumes:
      - ./server:/home/node/app/server
      - ./test:/home/node/app/test
    environment:
      - NODE_ENV=test
