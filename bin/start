#!/bin/bash

DIR="$( cd -P "$( dirname "$0" )" && pwd )"
cd $DIR
cd ..

abort() {
	printf "\n  \033[31mError: $@\033[0m\n\n" && exit 1
}

docker-compose up $1 -d dev redis
if test $? -ne 0; then
	abort "Docker Compose Failed"
fi

exit 0
